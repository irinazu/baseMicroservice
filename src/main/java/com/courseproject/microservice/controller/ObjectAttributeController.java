package com.courseproject.microservice.controller;
/*
import com.courseproject.microservice.model.Object;
import com.courseproject.microservice.model.ObjectAttribute;
import com.courseproject.microservice.service.ObjectAttributeService;
import org.springframework.web.bind.annotation.*;

import java.util.List;

@RestController
@RequestMapping("/api/objectAttributes")
public class ObjectAttributeController {

    ObjectAttributeService attributeService;

    public ObjectAttributeController(ObjectAttributeService attributeService) {
        super();
        this.attributeService = attributeService;
    }

    @PostMapping
    ObjectAttribute saveObject(@RequestBody ObjectAttribute object){

        return attributeService.saveObjectAttribute(object);
    }

    @GetMapping
    List<ObjectAttribute> getAllObject(){
        return attributeService.getAllObjectAttribute();
    }
}*/
