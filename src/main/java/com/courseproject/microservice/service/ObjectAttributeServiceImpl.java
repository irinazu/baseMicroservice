package com.courseproject.microservice.service;
/*
import com.courseproject.microservice.model.ObjectAttribute;
import com.courseproject.microservice.repository.ObjectAttributeRepository;
import org.springframework.stereotype.Service;

import java.util.List;

@Service
public class ObjectAttributeServiceImpl implements ObjectAttributeService{

    ObjectAttributeRepository attributeRepository;

    public ObjectAttributeServiceImpl(ObjectAttributeRepository attributeRepository) {
        this.attributeRepository = attributeRepository;
    }

    @Override
    public ObjectAttribute saveObjectAttribute(ObjectAttribute object) {
        return attributeRepository.save(object);
    }

    @Override
    public List<ObjectAttribute> getAllObjectAttribute() {
        return attributeRepository.findAll();
    }
}
*/